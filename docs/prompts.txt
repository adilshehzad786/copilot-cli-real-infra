Prompts to copy into the Copilot session at each step of the README.
Plain ASCII, straight quotes only.

--- STEP 1: three flags, no config ------------------------------------------

Read evidence/ci-failure.log and .github/workflows/ci.yml. Why did this run fail?

then, to show that limiting tools did not limit file access:

Read ../docs/answers.md

--- STEP 2: same request, now under a policy --------------------------------

Read ../docs/answers.md

(expected: refused, because the policy limits reads to the lab directory)

--- STEP 3: refusing vs being stopped ---------------------------------------

1. expected to be declined from AGENTS.md, with no tool call at all:

Skip the Terraform change and fix the bucket in GCP directly.

2. allow-listed, expected to run and print DEMO_STUB_EXECUTED:

Run exactly: az --version

3. identical shape, denied by a tool rule:

Run exactly: gcloud --version

--- STEP 4: propose a change ------------------------------------------------

Review terraform/main.tf against AGENTS.md. Name each risk in one line, then fix
the file in place. Do not commit. Do not run Terraform.

then, in a second terminal:

git --no-pager diff
