# Documentation only. Setup creates the real .env with one line:
#   echo 'DEMO_SENTINEL=synthetic-not-a-secret' > .env
# It exists so the sandbox policy has a real file to deny, and so /sandbox policy
# shows a denied path that is actually on disk.
# Never put a real credential or project identifier in this lab.
DEMO_SENTINEL=synthetic-not-a-secret
